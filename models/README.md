# Model Directory

Due to GitHub file size limits, the trained YOLO `.pt` files are not included in this repository.

To run this project:
1. Download or train your YOLO weights.
2. Place your primary weights file here as `highpothole.pt`.
3. (Optional) Place a fallback weights file here as `lessmodel.pt`.
